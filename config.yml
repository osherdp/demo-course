title: 'Demo course '
description: 'Just checking out how it works '
template:
  repo: demo-course-template
  name: demo-course
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
